# Ferramentas

Tribunal TJSP: Lista de Processos (eproc) expõe 1 ferramenta (todas somente leitura).

### 1. `tribunal_tjsp_eproc_lista_consultar`
**Input**: `cpf` (opcional), `cnpj` (opcional), `nome_parte` (opcional), `oab` (opcional)

Tribunal TJSP: Lista de Processos (eproc), consulta em fonte oficial.

## Prompts de exemplo

```
Consultar Tribunal TJSP: Lista de Processos (eproc)
```
